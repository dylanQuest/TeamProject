@page
@model TeamProject.Pages.Tickets.IndexModel
@{
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h3 class="text-primary">Tickets</h3>
        </div>
        <div class="col-6">
            <a asp-page="Create" class="btn btn-primary">
                Add New Ticket
            </a>
        </div>
    </div>
</div>

<br /> <br /> <br />

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Ticket Number</th>
            <th>Booking</th>
            <th>Type</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model.Tickets)
        {
            <tr>
                <td width="40%">@obj.Id</td>
                <td width="40%">@obj.bookingId</td>
                <td width="40%">@obj.typeId</td>
                <td>
                    <a asp-page="Edit" asp-route-id="@obj.Id">Edit</a>
                </td>
                <td>
                    <a asp-page="Delete" asp-route-id="@obj.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>